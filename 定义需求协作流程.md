# 定义需求协作流程

## 当前目标

把设计工作台部署成公开网站，让所有访问者打开同一个网址后，默认使用站点服务器里的 API 配置，不需要每个人自己填写 Key。

## 目前采用的部署结构

- 前端页面：`site/`
- API 代理：`netlify/functions/`
- 发布配置：`netlify.toml`
- 本地密钥文件：`config.local.json`、`API配置填写.json`

公开网站只部署 `site/` 和 `netlify/functions/`。本地密钥文件不能上传到 GitHub，也不能传到 Netlify 发布包里。

## 需要用户协作的地方

1. 上传代码到 GitHub。
   - 推荐用 GitHub Desktop 或 Git 命令上传整个项目。
   - 不要手动拖拽上传 `config.local.json`、`API配置填写.json`、任何 `.zip` 备份包。

2. 连接 Netlify。
   - 在 Netlify 新建站点，选择 GitHub 仓库。
   - Build command 留空。
   - Publish directory 使用 `site`。
   - Functions directory 使用 `netlify/functions`，这个已经写在 `netlify.toml`。

3. 在 Netlify 里设置环境变量。
   - `DESIGN_TAVILY_KEY`：填写本地 Tavily Key。
   - `DESIGN_GEMINI_KEY`：填写本地 Gemini Key。
   - `GEMINI_MODEL`：填写 `gemini-2.0-flash` 或当前要用的模型名。

4. 部署后把 Netlify 网址发回来。
   - 我会继续做公网测试。
   - 测试范围包括页面打开、静态资源、查竞品 API、推 Prompt API、查差异本地功能、移动端基础布局。
   - 如果发现问题，我会继续修代码，并让 Netlify 重新部署。

## 已完成的稳定性修正

- API 代理现在优先读取 Netlify 服务器环境变量。
- 访客浏览器里的旧 Key 只作为兜底，不会覆盖公共站点配置。
- 本地密钥文件已从发布目录和 Git 上传范围中排除。

## 当前阻塞点

我这边没有你的 Netlify / GitHub 登录权限，也没有可用的 Netlify 命令工具，所以不能直接生成最终公网网址。下一步需要你先把安全代码上传到 GitHub，或在 Netlify 里完成授权。
