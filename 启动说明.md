# 本地启动说明

这是一个纯本地网页工作站，不调用 API，也不会上传图片。

## 启动

1. 打开这个文件夹。
2. 运行：

```bash
python3 server.py
```

3. 浏览器打开：

```text
http://127.0.0.1:4173
```

如果 4173 已经被占用，终端会自动显示新的本地地址，比如 `http://127.0.0.1:4174`。

## 数据保存

- 提示词、项目、配色和图片会保存在当前浏览器的本地存储里。
- 换浏览器或清理浏览器数据后，本地资料可能会消失。
- 可以在「项目」页点击「导出备份」保存一份 JSON 备份。
